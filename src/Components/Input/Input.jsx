import "./Input.css";

const Input = () => {
    return (null
        // <input type={type} name="title" className={cn({["warning"]: !isValid?.title})} ref={titleRef}/>
    );
};

export default Input;